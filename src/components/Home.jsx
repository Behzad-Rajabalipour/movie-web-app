import React, { Component } from 'react';
import Slider from "./Slider";

// Home component, rendering the Slider component
export default class Home extends Component {
    render() {
        return (
            <>
                {/* Render the Slider component */}
                <Slider />
            </>
        );
    }
}
